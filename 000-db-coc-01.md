# 🏫 Sunrise School Result Management - Final Structure

### ১. নতুন এবং সহজতর টেবিল ডিজাইন (Simplified)

আমরা সব অপ্রয়োজনীয় কলাম (MCQ, PR, TUT) বাদ দিয়েছি। এখন শুধু `obtained_marks` এবং `highest_marks` থাকবে, যা আপনার ছবির সাথে সামঞ্জস্যপূর্ণ ।

| টেবিলের নাম | কলাম ও বর্ণনা |
| --- | --- |
| **`students`** | <br>`image` (Nullable), `reg_no`, `name`, `father_name`, `mother_name`, `present_address`. 

 |
| **`marks`** | <br>`obtained_marks`, `highest_marks`, `letter_grade`, `gp`. 

 |
| **`grading_system`** | গ্রেড রেঞ্জ (যেমন: ৮০-১০০ = A+)। 

 |

---

### ২. 🚀 Database Refresh Script (Production Ready)

এই স্ক্রিপ্টটি আপনার টিমের জন্য চূড়ান্ত গাইড। এটি ব্যবহার করে প্রগ্রেস রিপোর্টের সকল ক্যালকুলেশন করা সম্ভব।

```sql
-- ১. ডাটাবেস তৈরি
DROP DATABASE IF EXISTS school_management_system;
CREATE DATABASE school_management_system;
USE school_management_system;

-- ২. কনফিগারেশন টেবিল
CREATE TABLE classes (id INT PRIMARY KEY, class_name VARCHAR(50));
CREATE TABLE sections (id INT AUTO_INCREMENT PRIMARY KEY, section_name VARCHAR(10));
CREATE TABLE exams (id INT PRIMARY KEY, exam_name VARCHAR(50));

-- ৩. স্টুডেন্ট টেবিল (মুনতাসিরের প্রোফাইল অনুযায়ী)
CREATE TABLE students (
    id INT AUTO_INCREMENT PRIMARY KEY,
    reg_no VARCHAR(20) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,
    father_name VARCHAR(100),
    mother_name VARCHAR(100),
    image VARCHAR(255) NULL, -- শুরুতে ব্ল্যাঙ্ক রাখা যাবে
    birth_date DATE NULL,
    present_address TEXT
);

-- ৪. সাবজেক্ট টেবিল (Highest Marks সহ)
CREATE TABLE subjects (
    id INT AUTO_INCREMENT PRIMARY KEY,
    class_id INT,
    subject_name VARCHAR(100),
    total_marks_limit INT DEFAULT 100 -- যেমন: ইংলিশ ২য় পত্রে ৫০
);

-- ৫. মার্কস টেবিল (ক্লিন এবং সিম্পল)
CREATE TABLE marks (
    id INT AUTO_INCREMENT PRIMARY KEY,
    student_id INT,
    class_id INT,
    section_id INT,
    subject_id INT,
    exam_id INT,
    academic_year YEAR,
    obtained_marks INT DEFAULT 0,
    highest_marks INT DEFAULT 0, -- ওই সাবজেক্টে ক্লাসের সর্বোচ্চ নম্বর
    letter_grade VARCHAR(2),
    gp DECIMAL(3,2),
    FOREIGN KEY (student_id) REFERENCES students(id),
    FOREIGN KEY (class_id) REFERENCES classes(id)
);

```

---

### ৩. ক্যালকুলেশন লজিক (How to Calculate GPA/CGPA/Position)

আপনার দেওয়া পিডিএফ রিপোর্ট অনুযায়ী ক্যালকুলেশনগুলো এভাবে কাজ করবে:

* 
**GPA (Single Exam):** নির্দিষ্ট পরীক্ষার মোট `GP` কে মোট বিষয়ের সংখ্যা দিয়ে ভাগ করে বের হবে ।


* 
**C.G.P.A (Final Result):** তিনটি সেমিস্টারের মোট প্রাপ্ত নম্বর যোগ করে ৩ দিয়ে ভাগ করে প্রাপ্ত গড় নম্বর থেকে গ্রেড বের হবে ।


* 
**Position (Merit Position):** এটি পুরো সেকশন বা ক্লাসের সকল ছাত্রের `Total Obtained Marks` এর উপর ভিত্তি করে `RANK()` ফাংশন দিয়ে বের করা হয় ।



---

### ৪. প্রগ্রেস রিপোর্ট ভিউ (SQL Query)

আপনার পিডিএফ-এর মতো সব ডাটা একবারে টেনে আনার জন্য এই কুয়েরিটি ব্যবহার হবে:

```sql
SELECT 
    sub.subject_name,
    MAX(CASE WHEN m.exam_id = 1 THEN m.obtained_marks END) AS '1st_Sem_Mark',
    MAX(CASE WHEN m.exam_id = 1 THEN m.gp END) AS '1st_Sem_GP',
    MAX(CASE WHEN m.exam_id = 2 THEN m.obtained_marks END) AS '2nd_Sem_Mark',
    MAX(CASE WHEN m.exam_id = 3 THEN m.obtained_marks END) AS 'Annual_Mark',
    SUM(m.obtained_marks) AS 'Yearly_Total'
FROM marks m
JOIN subjects sub ON m.subject_id = sub.id
WHERE m.student_id = 1 AND m.academic_year = 2025
GROUP BY sub.subject_name;

```


আপনার চিন্তা একদম সঠিক। প্রোজেক্ট যখন কেউ ভেরিফাই করবে, তখন যদি ডাটাবেসে মাত্র ২-৩টা এন্ট্রি থাকে, তবে সিস্টেমের আসল ক্ষমতা (যেমন: র‍্যাঙ্কিং, জিপিএ ক্যালকুলেশন, প্রগ্রেস রিপোর্ট) বোঝা যায় না।

ভেরিফিকেশন সহজ করার জন্য আমি **মুনতাসির আহমেদ** সহ মোট ১০ জন স্টুডেন্টের ৩টি পরীক্ষার কমপ্লিট ডাটা (Real Mock Data) দিয়ে স্ক্রিপ্টটি সাজিয়ে দিচ্ছি। এটি রান করলে যেকোনো টিউটর বা ক্লায়েন্ট এক পলকেই সিস্টেমের লজিক বুঝতে পারবে।

---

# 🏫 Sunrise School Result Management - Verification Ready SQL

এই কোডটি রান করলে আপনার সিস্টেমে **১০ জন স্টুডেন্ট** এবং তাদের **৩টি সেমিস্টারের** রেজাল্ট তৈরি হবে। 

```sql
-- ১. ডাটাবেস ও কনফিগারেশন
DROP DATABASE IF EXISTS sunrise_school_system;
CREATE DATABASE sunrise_school_system;
USE sunrise_school_system;

CREATE TABLE classes (id INT PRIMARY KEY, class_name VARCHAR(50));
CREATE TABLE sections (id INT AUTO_INCREMENT PRIMARY KEY, section_name VARCHAR(10));
CREATE TABLE exams (id INT PRIMARY KEY, exam_name VARCHAR(50));

-- ২. [cite_start]স্টুডেন্ট টেবিল (মুনতাসিরের ডাটা অনুযায়ী) [cite: 1, 6, 7, 8, 9, 11]
CREATE TABLE students (
    id INT AUTO_INCREMENT PRIMARY KEY,
    reg_no VARCHAR(20) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,
    father_name VARCHAR(100),
    mother_name VARCHAR(100),
    image VARCHAR(255) NULL, 
    present_address TEXT
);

-- ৩. [cite_start]সাবজেক্ট টেবিল [cite: 12]
CREATE TABLE subjects (
    id INT AUTO_INCREMENT PRIMARY KEY,
    class_id INT,
    subject_name VARCHAR(100)
);

-- ৪. [cite_start]মার্কস টেবিল (MCQ/PR বাদ দিয়ে ক্লিন ভার্সন) [cite: 19, 21, 22, 23, 24]
CREATE TABLE marks (
    id INT AUTO_INCREMENT PRIMARY KEY,
    student_id INT,
    class_id INT,
    section_id INT,
    subject_id INT,
    exam_id INT,
    academic_year YEAR,
    obtained_marks INT DEFAULT 0,
    highest_marks INT DEFAULT 0,
    letter_grade VARCHAR(2),
    gp DECIMAL(3,2),
    FOREIGN KEY (student_id) REFERENCES students(id)
);

-- ৫. [cite_start]মাস্টার ডাটা ইনসার্ট [cite: 13, 14, 15, 16, 17, 18, 20]
INSERT INTO classes VALUES (3, 'Three');
INSERT INTO sections (section_name) VALUES ('A'), ('B');
INSERT INTO exams VALUES (1, '1st Semester'), (2, '2nd Semester'), (3, 'Annual');

INSERT INTO subjects (class_id, subject_name) VALUES 
(3, 'General Science'), (3, 'Bangla'), (3, 'Bangla 2nd'), (3, 'BGS'), 
(3, 'English'), (3, 'English 2nd'), (3, 'GK'), (3, 'Mathematics'), (3, 'Religion');

-- ৬. [cite_start]১০ জন মক স্টুডেন্ট ইনসার্ট [cite: 1, 6, 7, 11]
INSERT INTO students (reg_no, name, father_name, mother_name) VALUES 
('20250301', 'MUNTASIR AHMED', 'IFTEKHARUR RAHMAN', 'SHARMIN BEGUM'),
('20250302', 'RAFIAN KARIM', 'ABDUL KARIM', 'NASRIN AKTER'),
('20250303', 'SADIA ISLAM', 'MD. ISLAM', 'FATEMA BEGUM'),
('20250304', 'AHSAN HABIB', 'ZAHIRUL HAQUE', 'RINA BEGUM'),
('20250305', 'NISHAT TASNIM', 'KAMRUL HASSAN', 'SELINA AKTER'),
('20250306', 'TANVIR ALAM', 'MAHBUB ALAM', 'LUTFUN NAHAR'),
('20250307', 'ZAYED KHAN', 'ASAD KHAN', 'ROKSANA BEGUM'),
('20250308', 'MAYSHA RAHMAN', 'MOTIUR RAHMAN', 'SHAHANA BEGUM'),
('20250309', 'FAHIM FOYSAL', 'REZAUL KARIM', 'MONOWARA BEGUM'),
('20250310', 'NUSRAT JAHAN', 'ANWAR HOSSAIN', 'JAHANARA BEGUM');

-- ৭. [cite_start]অটোমেটেড মার্কস জেনারেশন (ভেরিফিকেশনের জন্য সেরা উপায়) [cite: 12, 19, 32, 33, 34, 35]
-- এটি প্রতিটি স্টুডেন্টের জন্য ৩টি পরীক্ষার রেজাল্ট অটো ক্যালকুলেট করে ইনসার্ট করবে
INSERT INTO marks (student_id, class_id, section_id, subject_id, exam_id, academic_year, obtained_marks, letter_grade, gp)
SELECT 
    s.id, 3, 1, sub.id, e.id, 2025,
    @m := (40 + FLOOR(RAND()*60)), -- ৪০ থেকে ১০০ এর মধ্যে রেন্ডম নম্বর
    CASE 
        WHEN @m >= 80 THEN 'A+' WHEN @m >= 70 THEN 'A' WHEN @m >= 60 THEN 'A-'
        WHEN @m >= 50 THEN 'B' WHEN @m >= 40 THEN 'C' ELSE 'F'
    END,
    CASE 
        WHEN @m >= 80 THEN 5.00 WHEN @m >= 70 THEN 4.00 WHEN @m >= 60 THEN 3.50
        WHEN @m >= 50 THEN 3.00 WHEN @m >= 40 THEN 2.00 ELSE 0.00
    END
FROM students s, subjects sub, exams e;

```

---

### প্রোজেক্ট ভেরিফিকেশন করার জন্য ৩টি 'ম্যাজিক' কুয়েরি

কেউ যখন চেক করবে, তখন আপনি নিচের কুয়েরিগুলো রান করে দেখাবেন:

১. প্রোগ্রেস রিপোর্ট (মুনতাসিরের ৩টি পরীক্ষার মার্কস এক লাইনে): 

```sql
SELECT 
    sub.subject_name,
    MAX(CASE WHEN m.exam_id = 1 THEN m.obtained_marks END) AS '1st_Sem',
    MAX(CASE WHEN m.exam_id = 2 THEN m.obtained_marks END) AS '2nd_Sem',
    MAX(CASE WHEN m.exam_id = 3 THEN m.obtained_marks END) AS 'Annual',
    SUM(m.obtained_marks) AS 'Grand_Total'
FROM marks m JOIN subjects sub ON m.subject_id = sub.id
WHERE m.student_id = 1 GROUP BY sub.subject_name;

```

২. মেরিট লিস্ট (পুরো ক্লাসের র‍্যাঙ্কিং): 

```sql
SELECT 
    s.name, 
    SUM(m.obtained_marks) AS Total, 
    ROUND(AVG(m.gp), 2) AS CGPA,
    RANK() OVER (ORDER BY SUM(m.obtained_marks) DESC) AS Position
FROM marks m JOIN students s ON m.student_id = s.id
GROUP BY s.id ORDER BY Position ASC;

```

৩. গ্রেডিং সিস্টেম সামারি (কতজন A+ পেয়েছে): 

```sql
SELECT letter_grade, COUNT(*) as Student_Count 
FROM marks WHERE exam_id = 3 GROUP BY letter_grade;

```

---
---
---
দুঃখিত ভাই, আমি বুঝতে পেরেছি আপনি একদম গোছানো এবং পূর্ণাঙ্গ একটি স্ক্রিপ্ট চাচ্ছেন যেটা একবারে রান করলে আপনার পুরো ডাটাবেস এনভায়রনমেন্ট তৈরি হয়ে যাবে। আপনার প্রয়োজন অনুযায়ী **৫ জন স্টুডেন্ট**, যারা **৩টি সেমিস্টারই** সম্পন্ন করেছে—এমন একটি "Ready-to-Test" ফুল এসকিউএল স্ক্রিপ্ট নিচে দেওয়া হলো।

এখানে আমি **মুনতাসির আহমেদসহ ৫ জন স্টুডেন্টের** জন্য ৩টি পরীক্ষার রেজাল্ট (৯টি সাবজেক্টের ওপর) ইনসার্ট করে দিয়েছি, যাতে আপনি প্রগ্রেস রিপোর্ট এবং র‍্যাঙ্কিং খুব সহজেই ভেরিফাই করতে পারেন।

---

# 🏫 Sunrise School Full Database Setup (5 Students, 3 Exams)

নিচের কোডটি কপি করে আপনার SQL এডিটর-এ রান করুন।

```sql
-- ১. ডাটাবেস ক্লিন এবং ক্রিয়েশন
DROP DATABASE IF EXISTS sunrise_school_system;
CREATE DATABASE sunrise_school_system;
USE sunrise_school_system;

-- ২. কনফিগারেশন টেবিল (Classes, Sections, Exams)
CREATE TABLE classes (id INT PRIMARY KEY, class_name VARCHAR(50));
CREATE TABLE sections (id INT AUTO_INCREMENT PRIMARY KEY, section_name VARCHAR(10));
CREATE TABLE exams (id INT PRIMARY KEY, exam_name VARCHAR(50));

-- ৩. সাবজেক্ট টেবিল (Sunrise Curriculum অনুযায়ী)
CREATE TABLE subjects (
    id INT AUTO_INCREMENT PRIMARY KEY,
    class_id INT,
    subject_name VARCHAR(100),
    FOREIGN KEY (class_id) REFERENCES classes(id)
);

-- ৪. স্টুডেন্ট টেবিল (ফটো অপশনাল রাখা হয়েছে)
CREATE TABLE students (
    id INT AUTO_INCREMENT PRIMARY KEY,
    reg_no VARCHAR(20) UNIQUE NOT NULL,
    name VARCHAR(100) NOT NULL,
    father_name VARCHAR(100),
    mother_name VARCHAR(100),
    image VARCHAR(255) NULL DEFAULT 'default_student.png',
    present_address TEXT
);

-- ৫. মার্কস টেবিল (ক্লিন স্ট্রাকচার)
CREATE TABLE marks (
    id INT AUTO_INCREMENT PRIMARY KEY,
    student_id INT,
    class_id INT,
    section_id INT,
    subject_id INT,
    exam_id INT,
    academic_year YEAR,
    obtained_marks INT DEFAULT 0,
    highest_marks INT DEFAULT 0,
    letter_grade VARCHAR(2),
    gp DECIMAL(3,2),
    FOREIGN KEY (student_id) REFERENCES students(id),
    FOREIGN KEY (class_id) REFERENCES classes(id)
);

-- ৬. মাস্টার ডাটা ইনসার্ট (Class 3)
INSERT INTO classes VALUES (3, 'Three');
INSERT INTO sections (section_name) VALUES ('A');
INSERT INTO exams VALUES (1, '1st Semester'), (2, '2nd Semester'), (3, 'Annual Examination');

[cite_start]-- সাবজেক্ট সেটআপ (ছবিতে থাকা ৯টি সাবজেক্ট) [cite: 12, 19]
INSERT INTO subjects (class_id, subject_name) VALUES 
(3, 'General Science'), (3, 'Bangla'), (3, 'Bangla 2nd part'), (3, 'BGS'), 
(3, 'English'), (3, 'English 2nd part'), (3, 'General Knowledge'), (3, 'Mathematics'), (3, 'Religion');

-- ৭. ৫ জন স্টুডেন্টের তথ্য ইনসার্ট
INSERT INTO students (reg_no, name, father_name, mother_name, present_address) VALUES 
('20250301', 'MUNTASIR AHMED', 'IFTEKHARUR RAHMAN', 'SHARMIN BEGUM', 'SYLHET'),
('20250302', 'RAFIAN KARIM', 'ABDUL KARIM', 'NASRIN AKTER', 'SYLHET'),
('20250303', 'SADIA ISLAM', 'MD. ISLAM', 'FATEMA BEGUM', 'SYLHET'),
('20250304', 'AHSAN HABIB', 'ZAHIRUL HAQUE', 'RINA BEGUM', 'SYLHET'),
('20250305', 'NISHAT TASNIM', 'KAMRUL HASSAN', 'SELINA AKTER', 'SYLHET');

-- ৮. ৫ জন স্টুডেন্টের ৩টি পরীক্ষার কমপ্লিট মার্কস ইনসার্ট (Mock Data)
-- এটি প্রতিটি স্টুডেন্টের জন্য ১৩৫টি রো তৈরি করবে (৫ স্টুডেন্ট * ৯ সাবজেক্ট * ৩ পরীক্ষা)
INSERT INTO marks (student_id, class_id, section_id, subject_id, exam_id, academic_year, obtained_marks, highest_marks, letter_grade, gp)
SELECT 
    s.id, 3, 1, sub.id, e.id, 2025,
    @m := (CASE 
        WHEN s.id = 1 THEN (80 + FLOOR(RAND()*20)) -- মুনতাসির টপার
        WHEN s.id = 5 THEN (33 + FLOOR(RAND()*40)) -- নিশাত একটু দুর্বল
        ELSE (50 + FLOOR(RAND()*40)) 
    END),
    (95 + FLOOR(RAND()*5)), -- Highest Mark
    CASE 
        WHEN @m >= 80 THEN 'A+' WHEN @m >= 70 THEN 'A' WHEN @m >= 60 THEN 'A-'
        WHEN @m >= 50 THEN 'B' WHEN @m >= 40 THEN 'C' WHEN @m >= 33 THEN 'D' ELSE 'F'
    END,
    CASE 
        WHEN @m >= 80 THEN 5.00 WHEN @m >= 70 THEN 4.00 WHEN @m >= 60 THEN 3.50
        WHEN @m >= 50 THEN 3.00 WHEN @m >= 40 THEN 2.00 WHEN @m >= 33 THEN 1.00 ELSE 0.00
    END
FROM students s, subjects sub, exams e;

-- ৯. টিচার এবং ব্লগ টেবিল (পূর্বের রিকোয়েস্ট অনুযায়ী)
CREATE TABLE teachers (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    designation VARCHAR(100),
    image VARCHAR(255) DEFAULT 'default_teacher.png'
);

CREATE TABLE blogs (
    id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    content TEXT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

```

---

### প্রোজেক্ট ভেরিফাই করার জন্য এসকিউএল টেস্ট কোড

আপনার টিমের কেউ যদি ডাটাবেস ভেরিফাই করতে চায়, তবে নিচের এই ১টি কুয়েরি রান করলেই সে পুরো ৫ জনের **Yearly Merit List** এবং **CGPA** দেখতে পারবে:

```sql
SELECT 
    s.name AS 'Student Name',
    SUM(m.obtained_marks) AS 'Total Marks (All Exams)',
    ROUND(AVG(m.gp), 2) AS 'Yearly CGPA',
    RANK() OVER (ORDER BY SUM(m.obtained_marks) DESC) AS 'Position'
FROM marks m
JOIN students s ON m.student_id = s.id
GROUP BY s.id, s.name
ORDER BY Position ASC;

```
 

 

